# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real values.

# Override the C++ pricing server URL (default: http://localhost:8080)
# REACT_APP_API_URL=http://localhost:8080
